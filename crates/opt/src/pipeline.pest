WHITESPACE = _{ " " | "\t" }

funcpass = { "sccp" }

funcpipeline = { "func" ~ "(" ~ (funcpass ~ ("," ~ funcpass)*)? ~ ")" }

modpass = {
    funcpipeline
}

modpipeline = { SOI ~ (modpass ~ ("," ~ modpass)*)? ~ EOI }
